# Environment files (contain API keys)
.env
*.env.local

# Personal config (each team member has their own)
config.yaml

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Node modules (if any)
node_modules/

# Claude Code local cache (root level only)
/.claude/

# But DO include plugin settings (for team sharing)
!client-pulse/.claude/
!client-pulse/.claude/settings.json
